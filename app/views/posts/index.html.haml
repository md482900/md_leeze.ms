%h1{:align =>'center'} Post Index
%h2 Listin all Post

%table.table
  %tr
    %th Title
    %th Text
  - @posts.each do |post|   
    %tr
      %td= post.title
      %td= post.text
= link_to 'New Post', new_post_path, :class => 'btn btn-link'
= link_to 'Back to Employees', employees_path, :class => 'btn btn-link'